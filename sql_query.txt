SELECT COUNT(DISTINCT song_id) AS ID, COUNT(DISTINCT client_id) AS Value
FROM students
WHERE CONVERT(datetime, SUBSTRING(PLAY_TS , 7, 4)+'/'+SUBSTRING(PLAY_TS , 4, 2)+'/'+SUBSTRING(PLAY_TS , 1, 2))="2016/08/10"
GROUP BY CLIENT_ID;